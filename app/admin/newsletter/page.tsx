'use client'
import { useState } from 'react'
export default function AdminNewsletterPage(){const [input,setInput]=useState('Hydration serum + spring tips + gift set promo');const [draft,setDraft]=useState('');return <main className='p-8 max-w-4xl mx-auto space-y-4'><h1 className='text-3xl font-serif'>AI Newsletter Draft Scaffold</h1><textarea className='w-full border rounded-xl p-3' rows={4} value={input} onChange={e=>setInput(e.target.value)} /><button className='px-4 py-2 rounded-full bg-rose-mauve text-white' onClick={()=>setDraft(`Subject: Hydration Notes from 서울\n\nHi Beauty Circle,\n\nThis week we focus on 수분 and glow layering...\n\n${input}`)}>Generate Draft</button><textarea className='w-full border rounded-xl p-3' rows={10} value={draft} onChange={e=>setDraft(e.target.value)} /></main>}
